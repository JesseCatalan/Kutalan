#include <stdio.h>
#include <getopt.h>
#include <stdio.h>

int main(int argc, char *argv[]) {
    if (argc != 2) {
        fprintf(stderr, "Invalid invocation! ./lab3a [image]\n");
        exit(1);
    }
}
